{
  "name": "faq-bot",
  "version": "0.1.0",
  "type": "module",
  "description": "A small FAQ bot with local matching and optional OpenAI fallback",
  "main": "src/index.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "android:build": "npm run android:sync && npm run android:open"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "better-sqlite3": "^12.4.1",
    "express": "^4.18.2",
    "google-auth-library": "^9.15.1"
  }
}
